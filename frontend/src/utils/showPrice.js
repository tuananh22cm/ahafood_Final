const showPrice = (price) => {
    // price=price+'.000'
    // return price?.toLocaleString("it-IT", {
    //   style: "currency",
    //   currency: "VND",
    // }
    // );
    return price
  };
  export default showPrice